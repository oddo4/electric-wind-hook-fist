<Window x:Class="mousespeed.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:mousespeed"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <Label Content="Mouse speed slider" HorizontalAlignment="Center"/>
            <Slider Width="200" HorizontalAlignment="Center" Minimum="{Binding MinValue}" Maximum="{Binding MaxValue}" Value="{Binding SpeedValue}"/>
            <Label Content="{Binding SpeedValue}"/>
        </StackPanel>
        
    </Grid>
</Window>
